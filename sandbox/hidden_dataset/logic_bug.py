def count_down(n):
    """
    Counts down from a given number to 1.

    Args:
        n (int): The number to start counting down from.

    Returns:
        None
    """
    while n > 0:
        print(n)
        n -= 1